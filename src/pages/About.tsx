// import { useTranslation } from "react-i18next";
import PagesHeader from "../components/PagesHeader/PagesHeader";

const About = () => {
  // const { t } = useTranslation();

  return (
    <>
      <PagesHeader title="about.title" subtitle="about.description" />
    </>
  );
};

export default About;
